# Master encryption key for securing stored tokens.
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
MCP_ENCRYPTION_KEY=

# Optional: path to store encrypted token file (defaults to ~/.claude-mcp/token.enc)
MCP_TOKEN_PATH=
